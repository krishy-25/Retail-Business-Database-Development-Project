SELECT PURCHASES.PUR#, PRODUCTS.NAME AS "product name", TO_CHAR(PUR_TIME, 'FMMonth DD, YYYY') AS PUR_TIME, PURCHASES.PAYMENT, PURCHASES.SAVING FROM PURCHASES
INNER JOIN PRODUCTS ON PRODUCTS.PID = PURCHASES.PID;